git config --global color.diff.meta "172"
git config --global color.diff.commit "100"
git config --global color.diff.frag "132"
git config --global color.diff.old "160"
git config --global color.diff.new "100"
git config --global color.diff.whitespace "160 reverse"
git config --global color.diff-highlight.oldNormal "160"
git config --global color.diff-highlight.oldHighlight "187 160"
git config --global color.diff-highlight.newNormal "100"
git config --global color.diff-highlight.newHighlight "187 100"
git config --global color.branch.current "172 reverse"
git config --global color.branch.local "172"
git config --global color.branch.remote "100"
git config --global color.status.added "172"
git config --global color.status.changed "100"
git config --global color.status.untracked "71"
