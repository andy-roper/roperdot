export PROMPT="%~ %# "
export PROMPT="%~ %# "
export PROMPT="%~ %# "
export PROMPT="%~ %# "
export PROMPT="%~ %# "
export PROMPT="%~ %# "
export PROMPT="%~ %# "
export PROMPT="%~ %# "
export PROMPT="%~ %# "
export PROMPT="%~ %# "
export PROMPT="%~ %# "
export PROMPT="%~ %# "
export PROMPT="%~ %# "
export PROMPT="%~ %# "
export PROMPT="%~ %# "
export PROMPT="%~ %# "
export PROMPT="%~ %# "
